# 🎯 Итоговая согласованная техническая спецификация: Режим Самоулучшения

Этот документ фиксирует финальную, полностью согласованную с вами техническую спецификацию для реализации автоматического режима саморазвития в проекте **OpenCode UI**.

---

## 🗺️ Итоговая архитектура сквозного цикла

```text
+------------------+    Клик "✦ Сессия"     +-----------------------------------------+
| Администратор    | ---------------------> | Чат ИИ + Очистка старых песочниц         |
+------------------+                        +-----------------------------------------+
                                                                 |
                                                                 v
+------------------+     Отображение в UI   +-----------------------------------------+
| Панель Workspace | <--------------------- | Копирование чистых файлов из Docker-src |
+------------------+                        +-----------------------------------------+
         |                                                       |
         | Пишет ТЗ                                              v
         |                                  +-----------------------------------------+
         +--------------------------------> | ИИ правит файлы в Sandbox (гибкий)     |
                                            +-----------------------------------------+
                                                                 |
                                                                 v
+------------------+    Статус на экране    +-----------------------------------------+
| Индикатор тестов | <--------------------- | Сервер сам гонит npm run test после прав|
| (Светофор)       |                        +-----------------------------------------+
         |                                                       |
         | Если тесты зеленые                                    v
         |                                  +-----------------------------------------+
         +--------------------------------> | Снимок БД -> Ветка si/* -> PR в GitHub  |
                                            +-----------------------------------------+
                                                                 |
                                                                 v
+------------------+    Корректировка       +-----------------------------------------+
| ИИ правит код за | <--------------------- | Воркер опрашивает GitHub Actions CI    |
| счет лога ошибок |                        | (Если CI ОК -> AutoMerge -> Деплой)     |
| (до 3 попыток)   |                        | (Если CI Упал -> Авто-фикс в чат)       |
+------------------+                        +-----------------------------------------+
```

---

## 📋 Подробное описание каждого шага

### ✦ Шаг 1. Инициализация чата и песочницы (Sandbox)
* **Пользовательский опыт:** Администратор нажимает на боковой панели кнопку **«✦ Сессия саморазвития»**. Чат инициализируется мгновенно.
* **Стиль старта:** Чат открывается классически пустым. Ассистент не шлет приветствий заранее и вежливо ожидает первого указания или технического задания (ТЗ) от администратора.
* **Безупречная зачистка (Teardown & Setup):**
  * Любой мусор от старых песочниц полностью стирается.
  * Песочница `/app/workspace/sessions/{sessionId}/workspace` создается с нуля.
  * Исходные файлы копируются из **чистого эталонного Docker-образа (`/app/workspace-src`)**, что гарантирует отсутствие накопленных багов и абсолютную чистоту исходников при старте.
* **Изолированные загрузки (Uploads):** Любые загружаемые пользователем файлы во время активного сеанса саморазвития попадают **строго в папку песочницы сессии** (`sessions/{id}/workspace/uploads`), чтобы ИИ видел их локально в воркспейсе, а живой сервер оставался чистым от мусора.

---

### ✦ Шаг 2. Разработка и автоматическое тестирование
* **Гибкий доступ к кодовой базе:** Ассистент имеет гибкий доступ к коду песочницы (может править `src/`, `public/`, `package.json`, `vite.config.ts`), что дает ему возможность проводить глубокий рефакторинг и подключать новые библиотеки из npm.
* **Автоматические локальные тесты (Continuous Integration):**
  * Администратору и ассистенту больше не нужно вручную запускать тесты.
  * Сервер автоматически запускает `npm run test` после каждого сохранения файла ИИ-агентом.
  * **Комбинированный UI-статус (Светофор + Карточка):**
    * В шапке чата отображается лаконичный цветной кружок-светофор (`🟡 Тестирование...` / `🟢 Все тесты пройдены` / `🔴 Ошибка в тестах`).
    * Если тесты или компиляция завалились, ассистент **автоматически выводит в чат красивую карточку с форматированным логом ошибок**, чтобы администратор мог сразу увидеть причину сбоя прямо в ленте диалога, не открывая дополнительные панели.

---

### ✦ Шаг 3. Создание PR и версионирование в Git
* **Название Git-ветки:** При отправке кода на GitHub создается информативное и структурированное название ветки вида:
  `si/{session-id}/{feature-slug}` (например, `si/ses_abc123/dark-sidebar-buttons`). Это делает историю веток на GitHub понятной и удобной для командной работы.
* **Сообщения Git-коммитов:** ИИ-ассистент автоматически генерирует емкие и структурированные коммит-сообщения на основе внесенных изменений (например, `feat: add dark theme to sidebar buttons`) для сохранения полной автономии процесса.

---

### ✦ Шаг 4. Облачный CI и петля авто-исправления
* **Авто-слияние на GitHub (Auto-Merge на Green CI):**
  * ИИ создает PR на GitHub. Если тесты GitHub Actions CI завершаются успешно, PR автоматически сливается (auto-merge), запускается деплой на VDS Timeweb, а ИИ-агент пишет отчет в чат о проделанной работе.
* **Форматирование логов CI в чате:**
  * Если сборка на GitHub Actions падает, воркер извлекает только **компактную карточку с выжимкой конкретной завалившейся ошибки** и постит её в чат. Это колоссально экономит контекстное окно и позволяет быстро локализовать баг.
* **Автономная петля самоисправления (Auto-Fix Loop):**
  * ИИ автоматически считывает ошибку из карточки, исправляет код в песочнице, коммитит и пушит фикс.
  * **Запрос помощи (Human-in-the-loop):** ИИ разрешено сделать максимум **3 автоматические попытки исправления** подряд. Если после этого CI продолжает падать, ассистент пишет в чат: *«Я застрял на ошибке компиляции в файле X. Пожалуйста, подскажи мне, как исправить, или отредактируй его сам в Workspace»* — вовлекая администратора в совместный отладчик.

---

### ✦ Шаг 5. Аварийный откат и Резервное копирование
* **Резервная копия ПЕРЕД каждым деплоем:**
  * Чтобы исключить малейший риск повреждения данных, сервер автоматически создает резервную копию базы данных SQLite прямо **перед каждым применением изменений** на живом сервере.
* **Цветовая связь по деплоям (Color-coded Sync):**
  * Каждая сборка UI и бэкап БД, созданные в рамках одного деплоя, подсвечиваются в панели управления общим цветовым тегом/маркером. Это позволяет сразу визуально определить их временное соответствие.
* **Выборочный ручной откат (Selective Rollback):**
  * В панели настроек `SettingsPanel` администратор получает полный контроль. Вы можете откатить только интерфейс (UI), только базу данных (БД) к определенному снимку, либо оба элемента одновременно и независимо.

---

## 📈 Отражение в планах
Этот согласованный с вами детальный план сохранен в репозитории как **`docs/AGREED_SELF_IMPROVE_SPEC_V2.md`**.

Все изменения зафиксированы и запушены на ваш GitHub. Проект полностью готов к дальнейшим победам!
